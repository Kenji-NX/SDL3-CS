name: Deploy

on:
  push:
    tags:
      - '*'

jobs:
  pack:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 8.0.x

      - name: Setup JDK 21
        uses: actions/setup-java@v6
        with:
          distribution: microsoft
          java-version: 21

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      - name: Install .NET workloads
        run: dotnet workload restore

      - name: Build & pack
        run: |
          dotnet pack SDL3-CS/SDL3-CS.csproj -c Release /p:Version=$(git describe --exact-match --tags HEAD) -o artifacts

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: Packages
          path: artifacts/Ryujinx.*.nupkg

      - name: Publish tagged release to nuget.org
        run: dotnet nuget push artifacts/Ryujinx.*.nupkg -s https://api.nuget.org/v3/index.json --api-key ${{secrets.NUGET_API_KEY}}
